class Mino {
  constructor(colors) {
    this.colors = new Array(colors.length);
    for (let j=0; j<colors.length; j++) {
      this.colors[i] = new Array(colors[i].length);
      for (let i=0; i<colors[i].length; i++) {
        this.colors[j][i] = colors[j][i];
      }
    }
  }
}