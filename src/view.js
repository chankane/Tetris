class View {
  constructor(holdBoard, mainBoard, nextBoard) {
    this.holdBoard = holdBoard;
    this.mainBoard = mainBoard;
    this.nextBoard = nextBoard;
  }


static repaintHoldBoard(colors) {
  
}

  static repaint(holdColors, mainColors, ) {
    ?
  }
}